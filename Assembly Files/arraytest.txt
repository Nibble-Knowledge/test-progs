START

BASEADDRESS 19

LET *CHIP_SELECT
LET *DATA_BUS
LET *STATUS_BUS

LET A BE 0
LET EN BE 13
LET B AS 4 BE 0
LET C AS 4

*STATUS_BUS BE 0b0100
*DATA_BUS BE 0xA

ASM LOD N_[E]
ASM STR STR1[1]
ASM LOD N_[A]
ASM STR STR1[2]
ASM LOD N_[6]
ASM STR STR1[3]
ASM LOD N_[0]
ASM STR STR1[4]

ASM LOD N_[F]
ASM STR 0d10000

ASM STARTLOOP:
ASM SUB16 A EN INTO C

ASM LOD C[0]
ASM JMP NOT1
ASM LOD N_[0]
ASM JMP YESLOOP

ASM NOT1:
ASM LOD C[1]
ASM JMP NOT2
ASM LOD N_[0]
ASM JMP YESLOOP

ASM NOT2:
ASM LOD C[2]
ASM JMP NOT3
ASM LOD N_[0]
ASM JMP YESLOOP

ASM NOT3:
ASM LOD C[3]
ASM JMP ENDLOOP

ASM YESLOOP:

ASM LOD N_[A]
ASM STR 0d10000

ASM LOD N_[F]
ASM STR1:
ASM STR 0d60000

#MODIFY THE STR INSTRUCTIONS ABOVE TO ITERATE THROUGH THE ARRAY

ASM INC16 STR1[1]


A BE INCREMENT A

ASM LOD N_[0]
ASM JMP STARTLOOP

ASM ENDLOOP:

END

